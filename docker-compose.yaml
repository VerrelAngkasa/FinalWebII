services:
  frontend:
    build: ./frontend
    ports:
      - 3000:80
    depends_on:
      - backend

  backend:
    build: ./backend
    ports: 
      - 5000:5000
    environment:
      - MONGO_URI=mongodb+srv://sja-user:sja123@cluster1.8oox96x.mongodb.net/db-sja?retryWrites=true&w=majority